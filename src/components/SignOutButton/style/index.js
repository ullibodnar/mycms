import styled from 'styled-components'

const SignOutButton = styled.button`
  border: none;
  background: transparent;
  color: #fff;
  font-size: inherit;
  text-decoration: underline;
`
SignOutButton.displayName = 'StyledSignOutButton'

export default SignOutButton
